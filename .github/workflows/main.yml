name: Build Artifact
on:
  push:
    branches: [master]
permissions:
  contents: read  # required for checkout action
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '16.18.1'
          cache: 'npm'
      - run: npm install
      - run: npm test
      - run: npm run lint
      - run: npm run build
      - name: Save Artifact
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: node-revisions-artifact
          path: |
            build
            package.json
            package-lock.json
          retention-days: 3
